---
name: distillation
description: Work out what a filed source or a logged decision changes in the syntheses, the pipeline or the draft, and propose the change on a branch. Use after triage has been committed, or when the user says distil, incorporate, what does this change.
---

Read `conventions/meta/workflows/distillation.md` and follow it exactly.

If you have not already read `conventions/` in this session, read it first.
`conventions/domain/` has the standing assumptions, notation and glossary;
`conventions/meta/` describes how this repository works.

The workflow file is the definition. This skill only makes it invokable by
name, and an agent without skills reads the same file directly.
