---
name: session-harvest
description: Record the decisions, questions and actions of a working session or a discussion with a collaborator, append decisions to the decision log, and propose downstream changes separately. Use at the end of a session or when the user says harvest, log this, record what we decided.
---

Read `conventions/meta/workflows/session-harvest.md` and follow it exactly.

If you have not already read `conventions/` in this session, read it first.
`conventions/domain/` has the standing assumptions, notation and glossary;
`conventions/meta/` describes how this repository works.

The workflow file is the definition. This skill only makes it invokable by
name, and an agent without skills reads the same file directly.
